"""Curunír analyst workbench: the operator surface over the mission store.

One store root already carries the whole stack — mission workflow, fabric
acquisition, semantic understanding, analytical intelligence — under a single
hash chain. This package adds the plane through which humans operate it:

  * typed, access-filtered projections of every family (server-side, before
    serialization; hidden objects are operationally nonexistent to a view);
  * attributable commands that wrap the canonical plane functions — the
    browser never mutates truth directly and never invents semantics;
  * collaborative annotations and dissent as first-class recorded objects;
  * the report/dossier engine: versioned structured outputs whose every
    sentence carries an epistemic status and a real evidence basis;
  * a self-contained browser UI (no external assets, no CDN, no build step).

The workbench renders truth produced by the canonical stores. It does not
create a second ontology and it cannot grant authority the command model
does not have.
"""

PACKAGE_VERSION = "curunir-workbench-v1"
