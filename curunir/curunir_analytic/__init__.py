"""The analytical layer over the evidence-bound world model: themes, narratives,
stakeholders, impact paths, forecasts, warnings and historical analogues.

Every analytical object is typed state in the same hash-chained log, carrying a
claim basis that descends to the evidence behind it. History accumulates rather
than being overwritten, and replay rebuilds the layer with no provider or
network.
"""

PACKAGE_VERSION = "curunir-analytic-0.1"
