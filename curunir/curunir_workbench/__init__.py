"""The analyst workbench: the plane humans use to operate a mission store.

It offers access-filtered views of every record family, signed commands that go
through the same store functions the rest of Curunír uses, annotations, reports
and a self-contained browser UI. It shows what the other stores hold and cannot
grant authority the command model does not already have.
"""

PACKAGE_VERSION = "curunir-workbench-v1"
